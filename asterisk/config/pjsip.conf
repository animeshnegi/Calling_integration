; Base PJSIP configuration.
; Static UDP/TCP transports are generated by entrypoint.sh.
; Database-managed extensions/providers are written only to the shared dynamic include.
; Never commit real SIP credentials to this repository.

#include pjsip.bootstrap.conf
#include dynamic/pjsip.dynamic.conf
