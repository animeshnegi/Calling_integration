[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
tlsenable = yes
tlsbindaddr = 0.0.0.0:8089
; For production, replace these with a trusted certificate/key mounted into the container.
tlscertfile = /etc/asterisk/keys/asterisk.pem
