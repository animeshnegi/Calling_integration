[general]
dateformat=%F %T.%3q %z

[logfiles]
; Keep logs on the container stream so Docker's bounded rotation policy applies.
; Do not write messages/security files into the container writable layer.
console => warning,error,security
